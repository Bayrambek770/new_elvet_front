# Environment variables for vetcare-pawsitive-connect
# Frontend (Vite) variables MUST be prefixed with VITE_

# API base URL including /api/v1/ suffix
# Dev example:
VITE_API_BASE=http://localhost:8000/api/v1/

# Optional: route to redirect to after logout
VITE_LOGOUT_REDIRECT=/auth
VITE_ADMIN_SHOULD_BE_REDIRECTED=None